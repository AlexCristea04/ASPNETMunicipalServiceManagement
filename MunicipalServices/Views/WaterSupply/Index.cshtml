@model IEnumerable<MunicipalServices.Models.WaterSupply>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DailyConsumption)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WaterQualityRating)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DailyConsumption)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WaterQualityRating)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.WaterSupplyId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.WaterSupplyId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.WaterSupplyId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
